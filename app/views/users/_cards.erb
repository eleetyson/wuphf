<% current_user.friends.recently_added.each do |friend| %>
  <div class="card">
    <h3><%= friend.name %></h3>
    <p>📧 <%= friend.email %></p>
    <p>☎️ <%= friend.phone %></p>
    <h4><a href="/">Edit</a></h4>
  </div>
<% end %>
